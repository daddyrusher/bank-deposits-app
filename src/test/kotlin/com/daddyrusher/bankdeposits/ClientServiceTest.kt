package com.daddyrusher.bankdeposits

import com.daddyrusher.bankdeposits.domain.Client
import org.junit.jupiter.api.Test
import org.springframework.beans.factory.annotation.Autowired
import org.springframework.boot.test.context.SpringBootTest

@SpringBootTest
class ClientServiceTest {

   /* @Test
    fun saveRecord() {
        val client = Client()
        client.setName("Maksim")
        client.setShortName("Zanin")
        client.setAddress("test address")
        client.setRulesForm("test rules")
        val client1: Client = repository.save(client)
        println(client1)
    }*/
}
